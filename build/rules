#!/usr/bin/env bash
set -euo pipefail
shopt -s globstar

cd "$(dirname "$0")/.."

export NODE_OPTIONS='--import=@danielx/hera-previous/register/tsc'
export PATH="$PWD/node_modules/.bin:$PATH"


civet source/cli.civet --ast <source/hera.hera >source/rules.json
